<UserControl x:Class="PatientManagement.PatientStorage.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:behaviors="clr-namespace:Infrastructure.Behaviors;assembly=Infrastructure"
             xmlns:local="clr-namespace:PatientManagement.PatientStorage.Views"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox  Text="{Binding SearchItem.DisplayValue,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"   VerticalAlignment="Top" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Search"  Command="{Binding SearchCommand}"  Margin="5"/>            
        </StackPanel>
    </Grid>
</UserControl>
