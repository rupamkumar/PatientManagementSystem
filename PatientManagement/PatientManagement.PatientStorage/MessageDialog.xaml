<controls:MetroWindow x:Class="PatientManagement.PatientStorage.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:PatientManagement.PatientStorage"
        mc:Ignorable="d" Background="#DDDDDD" WindowStartupLocation="CenterOwner"
        Height="140" SizeToContent="WidthAndHeight" MinWidth="300">

   
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="textBlock" Text="The Message Text" TextWrapping="Wrap" Margin="20"/>
        <StackPanel x:Name="ButtonsPanel"
                  Orientation="Horizontal"
                  Grid.Row="2"
                  HorizontalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Width" Value="75"/>
                </Style>
            </StackPanel.Resources>

        </StackPanel>
    </Grid>
</controls:MetroWindow>
