<UserControl x:Class="RupStoreAuthentication.Views.LoginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:behaviors="clr-namespace:Infrastructure.Behaviors;assembly=Infrastructure"
             xmlns:local="clr-namespace:RupStoreAuthentication.Views"
             mc:Ignorable="d" d:DesignWidth="400" Height="209">

    <UserControl.Resources>
        <ResourceDictionary Source="../Styles/Theme.xaml">

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="0,0,0,109" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label  Content="Username : " Grid.Row="0" Margin="5"/>
        <TextBox Name="txtUserName"  Text="{Binding Path=UserEntity.UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Width="250"/>
        <Label Content="Password : " Grid.Row="1" Margin="5"></Label>
        <PasswordBox  Name="txtPassword"    Grid.Column="1" Grid.Row="1" Width="250"/>

        <StackPanel Grid.Column="1" Grid.Row="2" VerticalAlignment="Bottom">
            <Button Content="Login" Width="100" HorizontalAlignment="Right" Command="{Binding LoginCommand}"  Click="LoginButton_Click"  />
        </StackPanel>
    </Grid>
</UserControl>
