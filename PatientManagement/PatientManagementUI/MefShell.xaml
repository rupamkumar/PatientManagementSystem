<controls:MetroWindow x:Class="PatientManagementUI.MefShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:prism="http://www.codeplex.com/prism"
        xmlns:inf="clr-namespace:Infrastructure;assembly=Infrastructure" 
        Title="Virtual Hospital" Height="450" Width="800" WindowStartupLocation="CenterScreen" WindowState="Maximized" Background="#333333"
        ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" Icon=".\Clinical_images.jpg">

    
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!--<RowDefinition Height="15" />-->
                <RowDefinition Height="*" />                                
            </Grid.RowDefinitions>
            <!--<Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>-->
            <Menu Background="#333333" Grid.Row="0" >               
                <MenuItem Header="Patient" Command="{Binding NavigateCommand}" CommandParameter="PatientView" />
                <MenuItem Header="Pharmacy" Command="{Binding NavigateCommand}" CommandParameter="PatientDetailsView" />
                <MenuItem Header="Command Centere" Command="{Binding NavigateCommand}" CommandParameter="CommandCentreView"/>
                <MenuItem Header="Theater Room" Command="{Binding NavigateCommand}" CommandParameter="ViewB"/>
                <MenuItem Header="Open Shell" Command="{Binding OpenShellCommand}" CommandParameter="MainView" />
                <MenuItem Header="Administration" Command="{Binding OpenShellCommand}" CommandParameter="AdministrationView" />
            </Menu>
            

            <!--<Grid>-->
            <!--<Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>-->
            <!--<Border Grid.Column="0" Grid.Row="1" Background="LightGray" MinWidth="150" Margin="5,36,0,5">
                <ItemsControl x:Name="NavigationItemsControl" prism:RegionManager.RegionName="MainNavigationRegion" Grid.Column="0" Margin="5" Padding="5" />
            </Border>-->
            <!--<ContentControl Grid.Row="1" Grid.Column="0" prism:RegionManager.RegionName="{x:Static inf:RegionNames.MainNavigationRegion}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="0,36,0,0"/>-->
            <ContentControl Grid.Row="2" Grid.Column="1" prism:RegionManager.RegionName="{x:Static inf:RegionNames.MainContentRegion}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="0,36,0,0"/>
        <!--</Grid>-->

        <!--<ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static inf:RegionNames.ContentRegion}"/>-->
        <!--<ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static inf:RegionNames.MainNavigationRegion}"/>-->
       
            <!--Background="{StaticResource IsChangedBackgroundBrush}"-->


            
        </Grid>
    </Grid>

</controls:MetroWindow>
