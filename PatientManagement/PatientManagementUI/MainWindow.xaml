<Window x:Class="PatientManagementUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://www.codeplex.com/prism"
        xmlns:inf="clr-namespace:Infrastructure;assembly=Infrastructure"       
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="Open Shell" Command="{Binding NavigateCommand}" CommandParameter="ViewA" />
            <MenuItem Header="PatientVault" Command="{Binding NavigateCommand}" CommandParameter="PatientDetailsView" />
            <MenuItem Header="ModuleVault" Command="{Binding NavigateCommand}" CommandParameter="ViewB"/>
        </Menu>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static inf:RegionNames.ContentRegion}"/>
        <ContentControl Grid.Row="2" prism:RegionManager.RegionName="{x:Static inf:RegionNames.MainContentRegion}"/>
    </Grid>
</Window>
