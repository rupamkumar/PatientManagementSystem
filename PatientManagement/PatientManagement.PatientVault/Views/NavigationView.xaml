<UserControl x:Class="PatientManagement.PatientVault.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PatientManagement.PatientVault.Views"
             MinHeight="250" MinWidth="350">

    <UserControl.Resources>
        <Style x:Key="NaviItemContainerStyle" TargetType="ContentPresenter">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Margin" Value="2"/>
        </Style>
        <DataTemplate x:Key="NaviItemTemplate" >
            
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="250"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Header="Patients">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Patient.LastName}" Background="Brown"
                    ItemContainerStyle="{StaticResource NaviItemContainerStyle}"
                    ItemTemplate="{StaticResource NaviItemTemplate}"
                      Margin="10"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
