<UserControl x:Class="PatientManagement.PatientVault.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://www.codeplex.com/prism"
             xmlns:view="clr-namespace:PatientManagement.PatientVault.Views"             
            MinHeight="250" MinWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <view:NavigationView Grid.Column="0" Grid.RowSpan="3" Width="Auto" />
        <view:PatientDetailsView Grid.Column="1" DataContext="{Binding PatientDetailsViewModel}" />
    </Grid>
</UserControl>
